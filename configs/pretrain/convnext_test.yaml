# Model Configuration
model:
  name: convnextv2_pico
  patch_size: 32
  decoder_depth: 1
  decoder_embed_dim: 512
  mask_ratio: 0.6

# Loss Configuration
loss:
  name: mse
  norm_pix_loss: true
  
# Data Configuration
data:
  input_size: 224
  data_path: "./data/pretrain/massgiss/train"
  eval_data_path: "./data/pretrain/massgiss/val"
  num_workers: 16

# Pre-training Hyperparameters
training:
  # Optimizer parameters
  optimizer: adamw
  weight_decay: 0.05
  beta1: 0.9
  beta2: 0.95
  # Learning Rate Schedule
  learning_rate: 1.e-3 # Base LR
  min_lr: 0.0 # Lower bound
  warmup_epochs: 1
  # Training loop
  epochs: 10 # Adjust as needed
  batch_size: 56 # Adjust based on GPU memory
  gradient_accumulation_steps: 1 # Gradient accumulation steps
  # Misc
  mixed_precision: true # Use mixed precision training
  seed: 42

# Logging & Checkpointing
logging:
  enable_wandb: true
  wandb_project: "topo-conv-mae-pretrain" # Your W&B project name
  wandb_run_name: "convnext_test" # Optional custom run name
  # wandb_run_id: "cnn_mae_bm_test" # Optional custom run id
  output_dir: "./output/pretrain/convnext_test" # Checkpoint dir
  val_interval: 1 # validation interval
  log_interval: 1 # wandb log interval
  include_vis: true # Include visualizations
  vis_interval: 1 # visualization interval
  checkpointing: false
